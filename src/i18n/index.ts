/**
 * Re-export i18n configuration and utilities for convenient imports.
 */
export { locales, defaultLocale, localeLabels, type Locale } from './config';
export { formatDate, formatNumber, formatCurrency } from './format';
